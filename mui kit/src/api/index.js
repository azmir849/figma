const baseUrl = "http://127.0.0.1:8000";

const loginUrl = `${baseUrl}/api/login`

//category
const categoryUrl = `${baseUrl}/api/admin/categorys`

//posts
const postUrl = `${baseUrl}/api/admin/posts`



export {
  baseUrl,

  loginUrl,

  categoryUrl,

  postUrl
  
};
